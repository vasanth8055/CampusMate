/**
 * Legacy path re-export — prefer `@/design-system` or `@/config/theme`.
 * Values mirror src/design-system for the features/ folder at repo root.
 */
export {
  brand,
  neutral,
  dark,
  textUtility,
  radius,
  radiusUtility,
  shadowUtility,
  spacing,
} from "../src/design-system";
