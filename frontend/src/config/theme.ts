/**
 * App-level theme configuration.
 * Re-exports the centralized design system for convenient imports.
 *
 * @example
 * import { brand, textUtility, radius } from "@/config/theme";
 */
export {
  brand,
  neutral,
  dark,
  cssVar,
  fontFamily,
  fontWeight,
  textScale,
  textUtility,
  spacing,
  spacingPx,
  radius,
  radiusUtility,
  shadow,
  shadowUtility,
  duration,
  easing,
  transition,
  zIndex,
  lightTheme,
  darkTheme,
  themes,
  getThemeTokens,
  getCssVar,
  applyThemeTokens,
  type ThemeName,
  type ThemeTokens,
  type TextScale,
  type RadiusToken,
  type ShadowToken,
  type SpacingToken,
  type ZIndexToken,
} from "@/design-system";
